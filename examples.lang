
def square(x):
    x * x

def absdiff(x,y):
    if (x < y):
       y - x
    else
       x - y

def quad(x):
    square(square(x))

def factorial(x):
    if (x == 0) then
       1
    else
       x * factorial(x-1)


def fib(x): if (x < 2) then 1 else fib(n-1) + fib(n-2)

def blah(x):
      let y = x*x in
  let z = absdiff(y,x+10) in
  let foo = z * y in
  if x < 0 then foo else y
